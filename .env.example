VITE_SUPABASE_URL=https://<your-project>.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Set to "true" only in local/dev builds to enable the phone+OTP QA bypass
# (see supabase/functions/certificate-test-login). Never set true in production.
VITE_CERTIFICATE_TEST_AUTH=false
